rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /spots/{fileName} {
      allow read: if true;
      allow write: if request.resource.size < 8 * 1024 * 1024  // 8 MB limit
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
