{
  "name": "spotfinder-ambient-tiles-builder",
  "private": true,
  "type": "module",
  "description": "Offline pipeline: Overpass extract -> GeoJSON -> PMTiles, for the ambient POI layer. Not part of the deployed app bundle.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "osmtogeojson": "^3.0.0-beta.5"
  }
}
