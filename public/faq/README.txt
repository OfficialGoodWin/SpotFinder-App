DROP YOUR IMAGES HERE
=====================
These images appear as step-by-step tutorial illustrations in the FAQ page.
Until you add your own, the app shows built-in SVG diagram fallbacks.

FILE NAMES (case-sensitive, must match exactly):
  plus-button.png      Step: tapping the green + button
  tap-map.png          Step: tapping the map to place a spot
  fill-form.png        Step: filling in the add-spot form
  add-photo.png        Step: adding a photo to a spot
  spot-appears.png     Step: the new spot appearing on the map
  open-detail.png      Step: tapping a spot to open its detail panel
  rate-spot.png        Step: tapping the stars to rate
  navigate.png         Step: the navigation panel with Start button
  route-modes.png      Step: the Drive / Bike / Walk mode selector
  voice.png            Step: the voice/mic button in the description field
  share.png            Step: the share button and share sheet
  map-layers.png       Step: the Layers button and style picker
  traffic-layer.png    Step: the Traffic map layer with colored roads

RECOMMENDED DIMENSIONS:
  600 × 338 px  (16:9 ratio) — wide enough to show UI context
  Keep files under 200 KB each (use TinyPNG or Squoosh to compress)
  PNG or JPEG both work fine

BEST PRACTICE:
  Screenshot directly from the app at 2× screen zoom
  Crop to show just the relevant UI element with a little context around it
  The images display at full width inside the FAQ accordion, max-height 220px
